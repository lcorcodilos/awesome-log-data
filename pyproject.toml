[project]
name = "awesome-log-data"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Lucas Corcodilos", email = "corcodilos.lucas@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "click>=8.4.2",
    "evtx>=0.12.1",
    "pydantic>=2.13.4",
    "pygrok>=1.0.0",
    "pyyaml>=6.0.3",
    "xmltodict>=1.0.2",
]

[build-system]
requires = ["uv_build>=0.9.21,<0.10.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "mypy>=2.3.0",
    "pytest>=9.1.1",
    "ruff>=0.16.2",
    "types-pyyaml>=6.0.12.20260815",
    "types-xmltodict>=1.0.1.20260518",
]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]

[tool.mypy]
strict = true
files = ["src", "tests"]

[[tool.mypy.overrides]]
module = ["pygrok.*"]
ignore_missing_imports = true
